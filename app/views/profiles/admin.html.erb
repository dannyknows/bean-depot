<%= render 'shared/nav', page_name: "product listings" %>
<%= render 'shared/profile_nav'%>

<div>
  <h1>users</h1>
  <% @users.each do |user| %>
    <div><%= user.fname + " " + user.lname %></div>
    <div><%= user.email %></div>
  <% end %>
</div>