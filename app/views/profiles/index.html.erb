<%= render 'shared/nav', page_name: "#{current_user.fname}" %>
<%= render 'shared/profile_nav'%>
<div>
  <%= link_to 'edit details', edit_profile_path(id: current_user.id) %>
  <p>email: <%= current_user.email %></p>
  <p>first name: <%= current_user.fname %></p>
  <p>last name: <%= current_user.lname %></p>
</div>
